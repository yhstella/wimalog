[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — 모든 라우트를 index.html로 보냅니다
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 정적 자산 캐싱
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
